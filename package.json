{
  "name": "smart-dictation",
  "private": true,
  "workspaces": [
    "packages/shared",
    "packages/server",
    "packages/web"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c blue,green \"npm run dev:server\" \"npm run dev:web\"",
    "dev:server": "npm -w packages/server run dev",
    "dev:web": "npm -w packages/web run dev",
    "build": "npm -w packages/server run build && npm -w packages/web run build",
    "typecheck": "tsc --noEmit -p packages/shared && tsc --noEmit -p packages/server && tsc --noEmit -p packages/web",
    "db:generate": "npm -w packages/server run db:generate",
    "db:push": "npm -w packages/server run db:push",
    "db:seed": "npm -w packages/server run db:seed"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "concurrently": "^9.1.0"
  }
}
